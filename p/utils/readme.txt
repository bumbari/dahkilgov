# script replace_palochka.py, created by Claude
# Сначала проверьте без изменений (покажет сколько замен будет)
python replace_palochka.py ./dahkilgov --dry-run

# Затем применить
python replace_palochka.py ./dahkilgov

# matcher.html - to match parables with proverbs, 3 proverbs to 1 parable.